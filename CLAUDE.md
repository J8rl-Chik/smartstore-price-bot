# 프로젝트 가이드

## Git

### 커밋

#### 단위

**한 커밋은 하나의 의도를 담는다.** 커밋 메시지를 "그리고"로 이어야 한다면 나눈다.

##### 함께 커밋한다

- 함수와 그 함수의 테스트
- 이름 변경과 그것을 참조하는 모든 파일 (중간 상태가 깨지지 않도록)
- 타입 정의 변경과 그 타입을 사용하는 코드

##### 나눠서 커밋한다

| 섞지 말 것                | 이유                                       |
| ------------------------- | ------------------------------------------ |
| 기능 변경 + 리팩토링      | 무엇이 동작을 바꿨는지 추적할 수 없다      |
| 리팩토링 + 포맷팅         | diff가 전체 파일로 번져 실제 변경이 묻힌다 |
| 서로 다른 도메인          | 되돌릴 때 하나만 골라낼 수 없다            |
| 프로덕션 코드 + 설정 파일 | 설정 변경은 영향 범위가 다르다             |

##### 되돌릴 수 있는 단위로 나눈다

**"이 커밋만 revert해도 프로그램이 동작하는가"**를 기준으로 삼는다.

동작하지 않는다면 너무 잘게 나눈 것이고, 되돌렸을 때 의도치 않은 것까지 사라진다면 너무 크게 묶은 것이다.

##### 예시

```
✕ refactor: productRow 분리하고 테스트 추가하고 타입 정리

○ refactor: productRow를 parseRawProductRow와 initRawProductRows로 분리
○ test: productRow 분리에 따른 테스트 재구성
○ refactor: RowValues 타입에서 readonly 제거
```

#### 제목

- 길이: 50자 이내로 작성한다.
- 양식: `타입: 제목`(예: feat: 로그인 기능 추가)
- 타입 목록
  - feat: 기능 추가
  - fix: 버그 수정
  - docs: 문서 수정 (README 등)
  - style: 코드 포맷팅, 세미콜론 누락 등 (코드 로직 변경 없음)
  - refactor: 동작 변경 없는 구조 개선
  - test: 테스트 코드 추가 및 수정
  - chore: 설정, 의존성, 빌드 추가

#### 본문

- 순서 없는 목록으로 작성한다.
- 길이: 한 줄에 72자 이내로 작성한다.
